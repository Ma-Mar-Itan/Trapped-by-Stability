# Runtime dependencies for the radiomics-stability pipeline.
# Pins reflect the scientific environment; do not downgrade casually as some
# results (KMeans, randomized PCA) are version-sensitive.

numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
statsmodels>=0.14
lifelines>=0.27
matplotlib>=3.7

# Harmonization. The code uses neuroCombat (`from neuroCombat import neuroCombat`).
# The previous requirements listed `neuroHarmonize` by mistake; reconciled here.
neuroCombat>=0.2.12
